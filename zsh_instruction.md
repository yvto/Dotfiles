# Instruction
To use that config you have to first install zsh and change it to your default shell. To do that first use your package manager to 
install zsh. 
On Fedora you have to type: `sudo dnf install zsh`.
Then change your default shell by typing: `chsh -s $(which zsh)`. Now restart your system.

