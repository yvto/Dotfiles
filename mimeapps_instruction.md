# Instruction 
In this repository file where are configured default apps is under the name: "mimeapps.list". It should be
placed in `~/.config` directory to work. 

# Adding new default apps
To add new default apps you can edit this file or use xdg-mime.
