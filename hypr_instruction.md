# Instruction
Install all of the files with:
```
sudo dnf install hyprland hyprlock hyprpaper hypridle
```
Now when you have everything installed you have to place directory called "hypr" in `~/Dotfiles`, and place
it in `~/.config` directory.
```
cp ~/Dotfiles/hypr/ -rf ~/.config
```

